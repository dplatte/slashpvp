<!DOCTYPE html>
<html>
<head>
  <title>/PvP</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta http-equiv="x-ua-compatible" content="ie=edge">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

</head>
<body>
<nav class="navbar navbar-light bg-faded">
	<a class="navbar-brand" href="">/PvP</a>
	<ul class="nav navbar-nav">
		<li class="nav-item active">
			<a class="nav-link" href="/">Home</a>
		</li>
		<li class="nav-item">
			<a class="nav-link" href="/ladder/us/3v3">Ladder</a>
		</li>
		<li class="nav-item">
			<a class="nav-link" href="">Stats</a>
		</li>
		<li class="nav-item">
			<a class="nav-link" href="">Teams</a>
		</li>
		<li class="nav-item dropdown pull-right">
			<button class="btn btn-secondary dropdown-toggle" title="<%= current_region.name %>" type="button" data-toggle="dropdown">
				<%= image_tag('icons/regions/' + current_region.abbr + '_icon.png', width: "30", alt: current_region.name) %>
			</button>
			<div class="dropdown-menu dropdown-menu-right text-center">
				<% Region.where.not(id: current_region.id).each do |region| %>
					<%= link_to(image_tag('icons/regions/' +  region.abbr + '_icon.png', width: "40"), {controller: "application", action: "set_region", region_id: region.id}, class: "dropdown-item", data: {toggle: "tooltip", placement: "left"}, title: region.name) %>
				<% end %>
			</div>
		</li>
		<li class="nav-item dropdown pull-right">
			<button class="btn btn-secondary dropdown-toggle" type="button" data-toggle="dropdown">
				<%= current_bracket.name %>
			</button>
			<div class="dropdown-menu dropdown-menu-right text-center">
				<% Bracket.where.not(id: current_bracket.id).each do |bracket| %>
					<%= link_to bracket.name, {controller: "application", action: "set_bracket", bracket_id: bracket.id}, class: "dropdown-item" %>
				<% end %>
			</div>
		</li>
	</ul>
</nav>


<%= yield %>

</body>
</html>
